# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.0.0'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in mixmax.gemspec
gemspec
